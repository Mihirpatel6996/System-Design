Step 1 — Start from a real problem

Let’s take something concrete: a payment system.

We want:

balance should never go negative
every change should be logged
only valid operations should change state
